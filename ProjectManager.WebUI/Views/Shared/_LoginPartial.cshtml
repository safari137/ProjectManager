@using Microsoft.AspNet.Identity

<ul class="nav navbar-nav navbar-right">

    @if (Request.IsAuthenticated)
    {
        <li>
            @Html.ActionLink("Hello " + User.Identity.GetUserName(), "Index", "UserInfo", new {area =""}, null)
        </li>
        <li>
            @Html.ActionLink("Logout", "Logout", "Auth", new {area = ""}, null)
        </li>
    }
    else
    {
        <li>
            @Html.ActionLink("Login", "Login", "Auth", new { area = "" }, null)
        </li>
    }
</ul>
